---
applyTo: ".github/workflows/**/*,**/*.{yml,yaml}"
description: "CI and YAML investigation routing"
---

For CI and config work:

- Prefer read-only diagnosis first.
- Use `yq` for structured YAML inspection.
- Use GitHub MCP or `gh run` for workflow run context.
