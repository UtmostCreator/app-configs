{
  "servers": {
    "codebase-memory-mcp": {
      "type": "stdio",
      "command": "codebase-memory-mcp",
      "args": ["--ui=false"],
      "cwd": "${workspaceFolder}",
      "env": {
        "CBM_ALLOWED_ROOT": "${workspaceFolder}"
      }
    }
  }
}
