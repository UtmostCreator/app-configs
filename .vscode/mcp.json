{
  "servers": {
    "codebase-memory-mcp": {
      "type": "stdio",
      "command": "codebase-memory-mcp",
      "args": [],
      "cwd": "${workspaceFolder}",
      "env": {
        "CBM_ALLOWED_ROOT": "${workspaceFolder}"
      }
    }
  }
}
