# Shared Memory

- Root policy lives in `AGENTS.md` and `docs/ai/project-context.md`.
- Keep `packages/contracts` backward compatible unless the task explicitly approves a coordinated rollout.
- Prefer package-local verification before root-level builds.
- Treat prompt files and commands as task entry points, not the canonical workflow source.
