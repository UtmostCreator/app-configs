# Verify Change Capability

Use this workflow to choose the smallest relevant proof for a changed behavior.

- prefer package-local tests before root checks
- treat `pnpm build` as smoke evidence
- separate commands run from checks still recommended
