# Plan Slice Command

Use this command when a task is multi-step, ambiguous, or architecture-affecting.

Workflow:

1. use the researcher or project-context flow if ownership is unclear
2. use the planner or architect agent to define a bounded slice
3. return acceptance criteria, risk level, and recommended next stage

Do not implement as part of this command unless the user explicitly asks for planning and implementation in one pass.
