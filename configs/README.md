# Config Profiles

This directory contains optional local developer environment profiles.

These files are not required for repository runtime behavior.

Use them as templates for shell, editor, terminal, and language-tool setup.

Do not store secrets, tokens, machine-specific credentials, or private paths here.
