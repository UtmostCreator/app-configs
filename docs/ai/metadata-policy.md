# Metadata Policy

Keep metadata generic and configurable.

- Project identifier fields should be configurable, not hardcoded.
- Ticket prefixes and URLs should be configurable.
- Runtime/tool surface capability claims must match actual support.

PR descriptions should include:

- risk level
- validation evidence
- commands not run and why
