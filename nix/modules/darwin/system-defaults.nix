{ ... }:
{
  # Phase 8 populates dock, finder, keyboard, trackpad, screenshot defaults.
  # Empty until then so `nix flake check` and `darwin-rebuild check` succeed
  # on a Mac as soon as the flake is wired up.
}
