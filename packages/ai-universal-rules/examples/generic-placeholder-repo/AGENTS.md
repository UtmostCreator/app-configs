# <PROJECT_NAME> - Repository Instructions

This example shows folder placement only. Leave placeholders intact here so the example remains generic.

Use the templates in `packages/ai-universal-rules/templates/` as the real source of truth when copying into a live repository.
