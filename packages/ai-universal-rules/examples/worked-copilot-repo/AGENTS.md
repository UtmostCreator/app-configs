# Acme Web - Shared Agent Guidance

- Treat `apps/web` and `packages/ui` as active product paths.
- Avoid editing `legacy-web/` unless a task explicitly targets it.
- Prefer focused Playwright or component tests before full workspace verification.
- Ask for approval before auth, billing, env, or dependency changes.
